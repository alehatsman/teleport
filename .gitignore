# Rust
/daemon/target/
/spike/target/
/cli/target/

# Dev daemon data (docs/09-frontend.md#dev-workflow) -- holds the auth token
/.dev-data/

# Web
/web/node_modules/
/web/dist/
/web/dist-ssr/
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# `provision apply tasks/findings.yml` writes one per crate. A build artifact
# for agents to read, never committed.
.gate/
