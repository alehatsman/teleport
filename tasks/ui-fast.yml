---
description: fast pre-commit web gate — lockfile drift, biome over staged, typecheck, ai-lint over staged, soft caps

# tq/fast over web/. No build, no suite, no network. The web half of
# tasks/ci-fast.yml.
steps:
  - name: fast gate
    use: ~/.cache/provision/tools/ts-quality/fast.yml
    props: { dir: web }
